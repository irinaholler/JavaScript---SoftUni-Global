function theLift(params) {
    let people = Number(params.shift());
    let addPeople = 0;

    for (let index = 1; index <= people; index++) {
        let numPeople = index;
        for (let j = 0; j < params.length; j++) {
            let liftSpace = Number(params[j]);
            
            if (liftSpace > 0 ) {
                
            }
        }
        
        
    }
    
}

theLift(["15", "0 0 0 0 0"])